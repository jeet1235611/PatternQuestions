#include <iostream>
using namesapce std;

int main() {
	int n;
	cout << "Enter the value of n\n";
	cin >> n;

	int upRow = n / 3;

	for(int row = 1; row <= n; row++){
		for(int space = 1; space <= upRow - row; space++){
			cout << " ";
		}
	if(row == 1 || row == n)
		for(int star = 1; star <= upRow; star++;
				cout << "*";
	else if(row <=
	
	}
}
